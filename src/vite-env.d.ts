/// <reference types="vite/client" />

// No provider configuration is read from build-time env vars. AI providers are
// configured entirely in the app (Settings → Providers) and persisted in the
// plaintext settings store; API keys are never inlined into the frontend bundle.
